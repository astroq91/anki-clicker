#include <zephyr.h>
#include <device.h>
#include <devicetree.h>
#include <drivers/gpio.h>

int main(void)
{
	const struct device *dev;
	int ret;

	// dev = device_get_binding("GPIO_0");
	// if (dev == NULL) {
	// 	return;
	// }
	// if (ret < 0) {
	// 	return;
	// }

	while (1) {
	}
}
